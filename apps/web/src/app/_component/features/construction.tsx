import { Card } from "@/components/ui/card";

export function Construction() {
    return (
        <Card className="flex flex-col items-center justify-center h-[300px] text-muted-foreground px-5">
            <span>Oops you're here a bit early. Rest of the site is under construction :)) </span>
        </Card>
    );
}